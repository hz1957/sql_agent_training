model:
  path: data/models/Qwen2.5-Coder-14B-Instruct

tokenizer:
  kind: hf

data:
  data_dir: data/spider
  train_file: train_spider.json
  validation_file: dev.json

output:
  sft_jsonl: artifacts/sft/train_qwen25_coder_14b_lora.jsonl
  checkpoint_dir: artifacts/checkpoints/sft_quality_lr3e5_r64_ep1

eval:
  sample_size: 100
  train_sample_size: 100
  sample_seed: 0

training:
  max_prompt_length: 2048
  max_response_length: 512
  learning_rate: 0.00003
  num_train_epochs: 3
  max_steps: 600
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 4
  gradient_checkpointing: true
  warmup_ratio: 0.03
  weight_decay: 0.0
  lr_scheduler_type: linear
  max_grad_norm: 1.0
  seed: 42
  data_seed: 42
  logging_steps: 10
  eval_strategy: steps
  eval_steps: 100
  per_device_eval_batch_size: 1
  eval_accumulation_steps: 1
  save_strategy: steps
  save_steps: 100
  bf16: true
  fp16: false
  report_to: none
  deepspeed: configs/deepspeed_zero2_h100.json

lora:
  enabled: true
  r: 64
  alpha: 128
  dropout: 0.05
  bias: none
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
