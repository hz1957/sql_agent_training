[project]
name = "sql-agent-training-workspace"
version = "0.1.0"
description = "Workspace for standalone Spider SQL agent training."
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["sql_agent_training"]
